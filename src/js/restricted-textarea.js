export const RestrictedTextarea = (() => {
  console.log("RestrictedTextarea");
  /*
   *   DOM refs
   *
   * */

  const init = () => {
    console.log("Init RestrictedTextarea");
    eventHandlers();
  };

  const eventHandlers = () => {};

  return {
    init,
  };
})();
